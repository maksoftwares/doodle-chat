name: Build Ephemera review preview

on:
  push:
    branches:
      - ephemera-review
    paths:
      - .ephemera-source/**
      - .github/workflows/ephemera-preview.yml
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: ephemera-review-preview
  cancel-in-progress: true

jobs:
  build-preview:
    runs-on: ubuntu-latest
    steps:
      - name: Check out review branch
        uses: actions/checkout@v4
        with:
          ref: ephemera-review

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: /tmp/ephemera-source/ephemera-chat/package-lock.json

      - name: Reconstruct and verify source archive
        shell: bash
        run: |
          set -euo pipefail
          rm -rf /tmp/ephemera-source
          mkdir -p /tmp/ephemera-source
          cat .ephemera-source/part-*.txt | tr -d '\r\n' | base64 --decode > /tmp/ephemera-source.tar.xz
          echo "83fc210ef0b8d97d4d60e6c2e0aac8bf3750ea8dc739da4376c75de37a665004  /tmp/ephemera-source.tar.xz" | sha256sum --check --strict
          tar -xJf /tmp/ephemera-source.tar.xz -C /tmp/ephemera-source

      - name: Install dependencies
        working-directory: /tmp/ephemera-source/ephemera-chat
        run: npm ci

      - name: Test, verify, and build
        working-directory: /tmp/ephemera-source/ephemera-chat
        run: npm run check

      - name: Publish complete static review build
        shell: bash
        run: |
          set -euo pipefail
          rm -rf ephemera-live
          mkdir -p ephemera-live
          cp -R /tmp/ephemera-source/ephemera-chat/dist/. ephemera-live/
          touch ephemera-live/.nojekyll
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A ephemera-live
          if git diff --cached --quiet; then
            echo "Preview is already current."
          else
            git commit -m "Publish complete Ephemera review build"
            git push origin HEAD:ephemera-review
          fi
